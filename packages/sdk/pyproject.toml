[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.build_meta"

[project]
name = "agent-debugger-sdk"
version = "0.1.0"
description = "Trace, debug and monitor your AI agents"
requires-python = ">=3.9"
dependencies = [
    "httpx>=0.27",
]
